services:
  redis:
    image: redis:7-alpine
    ports:
      - "6379:6379"
    volumes:
      - redis_data:/data
    command: redis-server --appendonly yes
    labels:
      - "com.kanban.frontend-url=http://localhost:3500"

volumes:
  redis_data:
